package parser

func ParsePageLimit(pageStr string, limitStr string) (*int, *int) {
	var page *int = nil
	var limit *int = nil

	if pageStr != "" {

	}

	if limitStr != "" {

	}

	return page, limit
}
